# Component Database

# Setup

Pre-Requisites:

- Ubuntu system running 20.04 LTS

1. Clone this repo using:
```bash
git clone --recurse-submodules https://github.com/fastrobotics/component_database.git
```
2. Run the following:

```bash
cd <repo>
./scripts/setup_ide.sh
```
